<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="4dp">

    <!-- Pats komentaras kaip kortelė -->
    <LinearLayout
        android:id="@+id/commentContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@drawable/bg_comment">

        <!-- Viršus: autorius + data -->
        <TextView
            android:id="@+id/commentHeaderTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Autorius • 2025-12-08 21:00"
            android:textStyle="bold"
            android:textSize="13sp"
            android:textColor="#555555" />

        <!-- Tekstas -->
        <TextView
            android:id="@+id/commentBodyTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Čia bus komentaro tekstas"
            android:textSize="15sp"
            android:textColor="#222222"
            android:paddingTop="2dp"
            android:paddingBottom="4dp" />

        <!-- Apačia: Reply mygtukas dešinėje -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="end">

            <TextView
                android:id="@+id/replyButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Atsakyti"
                android:textSize="12sp"
                android:textColor="@android:color/holo_blue_dark"
                android:padding="4dp" />
        </LinearLayout>

    </LinearLayout>

</LinearLayout>
